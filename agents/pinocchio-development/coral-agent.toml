edition = 3

[agent]
name = "solana-pinocchio"
version = "0.1.0"
description = "Pinocchio program development — zero-dependency, zero-copy Solana programs with 88-95% CU reduction vs Anchor. Account validation, CPI patterns, and Anchor migration."

readme = "Pinocchio program development — zero-dependency, zero-copy Solana programs with 88-95% CU reduction vs Anchor. Account validation, CPI patterns, and Anchor migration."
summary = "Pinocchio program development — zero-dependency, zero-copy Solana programs with 88-95% CU reduction vs Anchor. Account validation, CPI patterns, and Anchor migration."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
