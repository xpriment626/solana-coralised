edition = 3

[agent]
name = "solana-kamino"
version = "0.1.0"
description = "Kamino Finance — lending/borrowing (klend-sdk), automated liquidity strategies (kliquidity-sdk), oracle aggregation (scope-sdk), multiply/leverage, vaults, and obligation orders on Solana."

readme = "Kamino Finance — lending/borrowing (klend-sdk), automated liquidity strategies (kliquidity-sdk), oracle aggregation (scope-sdk), multiply/leverage, vaults, and obligation orders on Solana."
summary = "Kamino Finance — lending/borrowing (klend-sdk), automated liquidity strategies (kliquidity-sdk), oracle aggregation (scope-sdk), multiply/leverage, vaults, and obligation orders on Solana."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
