edition = 3

[agent]
name = "solana-meteora"
version = "0.1.0"
description = "Meteora Protocol — DLMM, DAMM v1/v2, dynamic bonding curves, Alpha Vaults, Zap, M3M3 Stake-for-Fee, Pool Farms, and token launches on Solana. Eight SDK suite."

readme = "Meteora Protocol — DLMM, DAMM v1/v2, dynamic bonding curves, Alpha Vaults, Zap, M3M3 Stake-for-Fee, Pool Farms, and token launches on Solana. Eight SDK suite."
summary = "Meteora Protocol — DLMM, DAMM v1/v2, dynamic bonding curves, Alpha Vaults, Zap, M3M3 Stake-for-Fee, Pool Farms, and token launches on Solana. Eight SDK suite."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
