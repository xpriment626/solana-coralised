edition = 3

[agent]
name = "solana-squads"
version = "0.1.0"
description = "Squads Protocol — V4 Multisig for team treasury management, Smart Account Program for account abstraction/programmable wallets, and Grid for stablecoin rails and fintech infrastructure."

readme = "Squads Protocol — V4 Multisig for team treasury management, Smart Account Program for account abstraction/programmable wallets, and Grid for stablecoin rails and fintech infrastructure."
summary = "Squads Protocol — V4 Multisig for team treasury management, Smart Account Program for account abstraction/programmable wallets, and Grid for stablecoin rails and fintech infrastructure."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
