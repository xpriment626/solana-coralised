edition = 3

[agent]
name = "solana-jupiter-swap"
version = "0.1.0"
description = "Jupiter Protocol — Ultra Swap, Lend, Perps, DCA/recurring buys, trigger orders, prediction markets, token lock, portfolio tracking, route optimisation, and Studio on Solana."

readme = "Jupiter Protocol — Ultra Swap, Lend, Perps, DCA/recurring buys, trigger orders, prediction markets, token lock, portfolio tracking, route optimisation, and Studio on Solana."
summary = "Jupiter Protocol — Ultra Swap, Lend, Perps, DCA/recurring buys, trigger orders, prediction markets, token lock, portfolio tracking, route optimisation, and Studio on Solana."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
