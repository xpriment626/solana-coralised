edition = 3

[agent]
name = "solana-manifest"
version = "0.1.0"
description = "Manifest CLOB DEX — on-chain order book, limit orders, reverse orders, global orders, Destiny vaults, wrapper/global account setup, and frontend integration on Solana."

readme = "Manifest CLOB DEX — on-chain order book, limit orders, reverse orders, global orders, Destiny vaults, wrapper/global account setup, and frontend integration on Solana."
summary = "Manifest CLOB DEX — on-chain order book, limit orders, reverse orders, global orders, Destiny vaults, wrapper/global account setup, and frontend integration on Solana."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
